# Base URL of the deployed backend (Vercel serverless functions)
# In dev this can be left empty — the Vite proxy handles /api/* calls
# In production set this to your Vercel backend URL, e.g.:
# VITE_API_URL=https://lto-ims-backend.vercel.app
VITE_API_URL=
